# Remove the file from all history in the git hub repository
git filter-repo --path .env.example --invert-paths

# Verirfying whether thr file is completely gone:
git log --all --full-history -- .env.example